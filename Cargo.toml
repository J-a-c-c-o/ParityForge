[package]
name = "ParityForge"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rand = "0.10.1"
rayon = "1.12.0"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
debug = false
