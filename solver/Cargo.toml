[package]
name = "solver"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rand = "0.10.1"
rayon = "1.12.0"

[dev-dependencies]
criterion = "0.4"

[[bench]]
name = "benchmark"
harness = false