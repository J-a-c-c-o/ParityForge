[package]
name = "test-solvers"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
peak_alloc = "0.3.0"
solver = { path = "../solver" }
